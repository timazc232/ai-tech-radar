# 后端接口文档 — REQ-1 AI Tech Radar 产品体验与社区信号扩展

- 状态：已完成，待评审
- 接口变更：涉及（无接口变化时改为“不涉及”并说明原因）
- 负责人：timazc232
- GitHub Issue：https://github.com/timazc232/ai-tech-radar/issues/1
- 服务/模块：radar-app `/api/settings`
- API 版本：现有本地 API
- OpenAPI/Swagger 地址或代码来源：`radar-app/app/api/settings/route.ts`
- 最后更新：2026-08-17

> 若不涉及接口变更，只保留基本信息、变更结论、兼容性和评审记录，并明确写 `接口变更：不涉及`。

## 1. 变更概览

- 新增/修改/废弃：GET/PUT settings 增加 X Bearer Token、Reddit Client ID/Secret 的脱敏读取与写入。
- 调用方及使用场景：本机设置页配置可选社交来源。
- 是否向后兼容：是，全部新字段可选。
- 生效条件和版本：本地代码启动后生效；无凭据时安全跳过。

## 2. 公共约定

### 鉴权与权限

- 认证方式：
- 权限点/角色：
- 租户与数据范围：
- 审计要求：

### 请求与响应约定

- Content-Type：
- Trace/Request ID：
- 时间、时区和金额格式：
- 分页、排序和空值约定：
- 统一响应结构：

## 3. 接口清单

| 方法与路径 | 名称 | 调用方 | 鉴权 | 幂等 | 兼容性 | 状态 |
|---|---|---|---|---|---|---|
| `GET /api/settings` | 读取脱敏配置 | Settings UI | 本机 | 是 | 兼容 | 修改 |
| `PUT /api/settings` | 保存配置 | Settings UI | 本机 | 字段覆盖 | 兼容 | 修改 |

## 4. 接口明细

### `POST /api/...`

#### 功能与业务规则

- 用途：
- 前置条件：
- 核心规则：
- 权限和数据范围：

#### 请求参数

| 位置 | 字段 | 类型 | 必填 | 约束/枚举 | 示例 | 说明 |
|---|---|---|---|---|---|---|

#### 请求示例

```http
POST /api/... HTTP/1.1
Content-Type: application/json
Authorization: Bearer <token>

{}
```

#### 响应字段

| 字段 | 类型 | 是否可空 | 示例 | 说明 |
|---|---|---|---|---|

#### 成功响应示例

```json
{
  "code": "0",
  "message": "成功",
  "data": {}
}
```

#### 失败响应与错误码

| HTTP 状态码 | 业务错误码 | 场景 | 调用方处理 | 是否可重试 |
|---:|---|---|---|---|

## 5. 幂等、超时、重试与限流

- 幂等键及有效期：
- 服务端重复请求行为：
- 超时预算：
- 重试条件、次数和退避：
- 限流维度和阈值：

## 6. 兼容、灰度与废弃

- 新旧字段兼容：
- 新旧版本并存：
- 灰度策略：
- 废弃时间线和调用方迁移：
- 回滚后的协议行为：

## 7. 契约验证

| 验证项 | 命令/Run | Commit | 结果 | 证据 |
|---|---|---|---|---|

## 8. 评审记录

| 调用方/审查者 | 结论 | 日期 | Issue/评论来源 |
|---|---|---|---|
